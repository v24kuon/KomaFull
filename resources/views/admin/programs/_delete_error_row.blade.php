<tr id="program-row-{{ $program->id }}" class="table-warning">
    <td colspan="8" class="text-danger">
        <div class="fw-semibold">{{ $program->name }} は削除できません。</div>
        <div class="small">{{ $message }}</div>
    </td>
</tr>
