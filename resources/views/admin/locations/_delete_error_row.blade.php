<tr id="location-row-{{ $location->id }}" class="table-warning">
    <td colspan="6" class="text-danger">
        <div class="fw-semibold">{{ $location->name }} は削除できません。</div>
        <div class="small">{{ $message }}</div>
    </td>
</tr>
