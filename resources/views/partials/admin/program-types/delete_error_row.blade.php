<tr id="program-type-row-{{ $programType->id }}" class="table-warning">
    <td colspan="5">
        <div class="text-danger" role="alert">
            <div class="fw-semibold">{{ $programType->name }} は削除できません。</div>
            <div class="small">{{ $message }}</div>
        </div>
    </td>
</tr>
